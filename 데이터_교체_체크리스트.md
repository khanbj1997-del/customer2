# P5-1 노트북 — 데이터 교체 후 수정 체크리스트

`data_v2_save.csv`로 입력 데이터를 바꾸는 작업 기준. 원칙: **1. 데이터 로드 셀 이외에는 최대한 건드리지 않는다.**

---

## ✅ 이미 완료됨 — 1. 데이터 로드 (셀 11)

- `df = pd.read_csv("data_v2_save.csv")`
- `CONFIG["target"] = "Churn"`
- `X_df` / `X` / `y` / `feature_names` 생성 및 `get_dummies` 처리까지 완료.

이 셀이 `X`, `y`, `CONFIG`, `feature_names`를 만들어주면, **나머지 셀들은 전부 이 네 변수만 참조하는 범용 코드**라서 데이터가 바뀌어도 그대로 재사용됩니다.

---

## 🔴 지금 반드시 고쳐야 실행되는 곳 (1곳)

### 셀 16 — `evaluate()` 함수 안의 빈칸 ④⑤⑥

데이터 교체와는 무관하게 원래 템플릿에서 학생이 채우도록 비워둔 부분인데, 이걸 채우지 않으면 0단계 베이스라인(셀 18)부터 바로 `NameError`가 납니다.

```python
"accuracy": round(accuracy_score(y_true, pred), 4),
"precision": round(precision_score(y_true, pred, zero_division=0), 4),   # ④
"recall": round(recall_score(y_true, pred, zero_division=0), 4),         # ⑤
"f1": round(f1_score(y_true, pred, zero_division=0), 4),                 # ⑥
```

바로 위 줄(`accuracy_score`)과 같은 패턴이고, 주석 힌트("zero_division=0 유지")를 그대로 적용하면 됩니다. `precision_score`, `recall_score`, `f1_score`는 이미 같은 셀 상단 `from sklearn.metrics import (...)`에서 import되어 있으므로 추가 import는 필요 없습니다.

---

## 🟡 실행엔 지장 없지만 정리해두면 좋은 곳 (선택)

### 셀 6 — `CONFIG["target"]` 표기 불일치

```python
"target": "churn",   # 소문자
```

실제 컬럼명은 `"Churn"`(대문자)이고, 셀 11에서 `CONFIG["target"] = "Churn"`으로 다시 덮어쓰기 때문에 **동작에는 문제없음**. 다만 "0. 프로젝트 정의" 단계에서 미리 최종 타깃명을 적어두는 취지에 맞게, 여기도 `"Churn"`으로 맞춰두면 나중에 헷갈리지 않습니다.

### 셀 6 — `"team"` 리스트

`["팀원A", "팀원B", "팀원C"]` → 실제 팀원 이름으로 교체 (코드 실행과는 무관, 과제 요건).

### 셀 7 — 네 문장 채우기 (마크다운)

"우리는 (   )를 입력으로 (   )를 예측한다" 등 4개 문장. 코드는 아니지만 과제 취지상(코드보다 문제 정의 먼저) 채우는 걸 권장.

---

## ⚪ 손댈 필요 전혀 없는 부분

| 구간 | 이유 |
|---|---|
| 2. 분할 (셀 14) | `X`, `y`, `CONFIG["task_type"]`, `CONFIG["test_size"]`, `CONFIG["val_size"]`만 사용 → 데이터 무관 |
| 3. 평가 함수 (셀 16, 빈칸 제외) | `y_true`, `y_prob` 형태만 가정 → 데이터 무관 |
| 4. 0단계 (셀 18) | `X_train`, `y_train`, `CONFIG["task_type"]` 사용 → 데이터 무관 |
| 5. 1단계 (셀 20~21) | 동일 |
| 6. 2단계 (셀 23~24) | `build_model()`이 `X_train.shape[1]`로 입력 차원을 자동으로 읽음 → 컬럼 수가 달라져도 그대로 동작 |
| 7. 정리 (셀 26~27) | `score_0/1/2`, `CONFIG`만 사용 → 데이터 무관 |

---

## 요약: 지금 해야 할 일

1. **(필수)** 셀 16 ④⑤⑥ 세 줄만 채운다.
2. **(선택)** 셀 6의 `target`을 `"Churn"`으로, `team`을 실제 이름으로 바꾼다.
3. 그 다음 셀을 위에서 아래로 순서대로 `Shift+Enter` 실행하면 끝까지 통과된다.
